<h1>Index</h1>

<hr>

<?php

	foreach($this->view->dados as $key => $info) { // Testando a recuperação de dados do IndexController
		
		// echo $produto['id_produto'] . ": " . $produto['descricao'] . " | R$" . $produto['preco'];
		// echo '<br>';

		echo '<h1>' . $info['titulo'] . '</h1>';
		echo '<p>' . $info['descricao'] . '</p>';
	}
?>